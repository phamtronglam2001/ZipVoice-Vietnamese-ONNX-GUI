# Demo assets for README

Drop files here — README links resolve automatically on GitHub:

| File | Purpose |
|------|---------|
| `../screenshot.png` | Gradio GUI screenshot (`![…](../screenshot.png)` in README) |
| `ref_voice.mp3` | Example reference voice (link in README demo table) |
| `output.wav` | Example synthesized output |
| `sample_text.txt` | Example input text (bundled) |

Suggested workflow: pick a bundled voice (e.g. `assets/sample_audio/Bá-Vinh.mp3`), run TTS with `sample_text.txt`, copy results here.

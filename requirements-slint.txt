# Slint GUI dependencies (separate from main requirements.txt).
# Install: .venv\Scripts\pip install -r requirements-slint.txt
# PyPI ships 1.9.x as pre-releases on Python 3.11; stable 1.9.0+ needs >=3.12.
slint>=1.9.0a1
